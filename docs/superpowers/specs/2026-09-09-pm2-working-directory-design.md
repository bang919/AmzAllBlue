# PM2 工作目录修复设计

## 目标

让 PM2 无论从哪个终端目录启动，均在项目根目录解析 `server.mjs` 与监控路径。

## 方案

在 `ecosystem.config.cjs` 的 `AmzAllBlue` 应用配置中设置 `cwd: __dirname`，并将入口写为相对于该工作目录的 `server.mjs`。

## 影响范围

仅变更 PM2 的启动路径解析。服务逻辑、环境变量、监控范围及其他运行行为保持不变。

## 验证

检查配置可被 Node.js 载入，并确认解析后的工作目录和脚本路径分别为项目根目录及其下的 `server.mjs`。
